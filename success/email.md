Your Aqtra Platform deployment has completed successfully.

**Aqtra Platform access**:  [https://${env.domain}/](https://${env.domain}/)  

Use the following credentials to access the Aqtra Studio:

**Aqtra Studio aceess:** [https://${env.domain}/studio/](https://${env.domain}/studio/)  
**Username:** ${globals.ADMIN_LOGIN}  
**Password:** ${globals.ADMIN_PASSWORD}  

If you need the deployment output values, open Azure Portal → Managed resource group → Deployments → Outputs

